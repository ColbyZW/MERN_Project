services:
  react:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - mongodb
      - node-server
    volumes:
      - ./frontend:/src
      - ignore:/src/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WDS_SOCKET_PORT=0
      - WATCHPACK_POLLING=true

  node-server:
    build: ./backend
    env_file:
      - ./.env
    ports:
      - "8000:8000"
    depends_on:
      - mongodb
    volumes:
      - ./backend:/src
      - ignore:/src/node_modules

volumes:
  ignore: